[project]
name = "effi"
version = "0.1.0"
description = "Effi reactor cycle analysis tools"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "plotly",
    "fastapi",
    "uvicorn[standard]",
    "orjson",
]

[project.optional-dependencies]
dev = ["jupyter"]
